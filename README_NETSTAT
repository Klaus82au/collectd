Netstat plugin can monitor every network device's stats,
for example collisions, rx/tx- _bytes, -_packets, -errors and so on. 

Example configuration

#LoadPlugin netstat
#TypesDB     "./types.db.netstat"
#<Plugin netstat>
#    NetDirPath "/sys/class/net/"
#    Statistics "tx_bytes"
#</Plugin>



